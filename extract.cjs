const fs = require('fs'); const html = fs.readFileSync('asura_html.txt', 'utf8'); const cheerio = require('cheerio'); const $ = cheerio.load(html); let found = false; $('astro-island').each((_, el) => { const compUrl = $(el).attr('component-url') || ''; if (compUrl.includes('Reader')) { found = true; fs.writeFileSync('asura_props.json', $(el).attr('props')); console.log('Saved'); } }); if (!found) console.log('No Reader');
